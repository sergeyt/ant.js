# TODO use node-di, log4js

loadConfig = ->
  # TODO nopt
  return {}

ant = (opts) ->
  # load config
  # load plugins, tasks
  # build modules
  # test modules
  return opts

# expose public API
module.exports = ant